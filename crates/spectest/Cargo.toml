[package]
name = "spectest"
version.workspace = true
authors.workspace = true
edition.workspace = true

[lib]
name = "spectest"
path = "src/lib.rs"

[dependencies]
spacewasm = { path = "../.." }
spacewasm-std = { path = "../spacewasm-std" }
wast = "220.0.0"
